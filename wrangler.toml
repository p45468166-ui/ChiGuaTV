# Cloudflare Workers 配置文件
name = "chiguatv"
main = "worker.js"
compatibility_date = "2024-03-01"
compatibility_flags = ["nodejs_compat"]

# 环境变量配置
[vars]
# 在这里添加需要的环境变量

# 开发环境配置
# 注意：local_port 在新版本 Wrangler 中不再支持，使用 --port 命令行参数代替

# 生产环境配置
[env.production]
# 生产环境特定配置
